---
import '../../styles/global.css';

export function getStaticPaths() {
  return [
    { params: { slug: 'terrier-transit' } },
    { params: { slug: 'bu-menu-optimizer' } },
    { params: { slug: 'spark-shift-bot' } },
    { params: { slug: 'bu-course-graph' } },
    { params: { slug: 'campus-events-api' } },
    { params: { slug: 'study-spot-finder' } },
  ];
}

const { slug } = Astro.params;
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{slug} - HackBU Projects</title>
  </head>
  <body class="min-h-screen flex items-center justify-center p-8">
    <div class="max-w-2xl text-center">
      <div class="text-6xl mb-4">🚧</div>
      <h1 class="font-display text-4xl text-spark-chartreuse mb-4">
        Project Page Coming Soon!
      </h1>
      <p class="text-lg text-spark-eggshell/80 mb-8">
        We're still building out individual project pages.<br />
        For now, check out the code on GitHub!
      </p>

      <div class="space-y-4">
        <a
          href={`https://github.com/bu-spark/${slug}`}
          class="inline-block px-6 py-3 bg-spark-chartreuse text-spark-black rounded-lg font-semibold hover:bg-spark-chartreuse/80 transition-colors"
        >
          View on GitHub →
        </a>
        <br />
        <a
          href="/"
          class="inline-block px-6 py-3 border-2 border-spark-teal text-spark-eggshell rounded-lg font-semibold hover:bg-spark-teal/20 transition-colors"
        >
          ← Back to HackBU OS
        </a>
      </div>
    </div>
  </body>
</html>
