---
import { db } from "../lib/db";
import "../styles/global.css";

// Fetch streaming event
const events = await db.event.findMany();
const streamEvent = events.find((e) => e.streamUrl);
const streamTime = streamEvent?.when || "Check Discord for schedule";
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>HackBU Live (Demo Placeholder)</title>
  </head>
  <body class="min-h-screen flex items-center justify-center p-8">
    <div class="max-w-2xl text-center">
      <div
        class="mb-4 inline-flex items-center gap-2 text-spark-eggshell/90 bg-spark-black/50 border-2 border-spark-orange/50 rounded-md px-3 py-2"
      >
        <span class="text-lg">⚠️</span>
        <span class="font-sans text-sm"
          ><strong>Demo Placeholder:</strong> This page is for demo purposes and
          is not the final streaming page.</span
        >
      </div>
      <div class="mb-8">
        <div class="text-8xl mb-4">📡</div>
        <h1 class="font-display text-5xl text-spark-chartreuse mb-4">
          No Stream Right Now
        </h1>
        <p class="text-xl text-spark-eggshell/80 mb-8">
          Check back during <strong>HackBU Live</strong> ({streamTime})<br />
          or follow us on Discord for stream notifications
        </p>
      </div>

      <div class="space-y-4">
        <a
          href="https://discord.gg/hackbu"
          class="inline-block px-6 py-3 bg-spark-chartreuse text-spark-black rounded-lg font-semibold hover:bg-spark-chartreuse/80 transition-colors"
        >
          Join Discord
        </a>
        <br />
        <a
          href="/"
          class="inline-block px-6 py-3 border-2 border-spark-teal text-spark-eggshell rounded-lg font-semibold hover:bg-spark-teal/20 transition-colors"
        >
          ← Back to HackBU OS
        </a>
      </div>

      <div class="mt-12 p-6 border border-spark-teal/40 rounded-lg text-left">
        <h2 class="font-display text-2xl text-spark-chartreuse mb-3">
          Upcoming Shows
        </h2>
        <ul class="space-y-2 text-spark-eggshell/80">
          <li>🎙️ <strong>API Tour:</strong> BU Dining Services API (TBA)</li>
          <li>🚀 <strong>Ship Room:</strong> TerrierTransit Team (TBA)</li>
          <li>💻 <strong>Live Bounty:</strong> Build a Discord Bot (TBA)</li>
        </ul>
      </div>

      <div
        class="mt-6 p-4 border-2 border-spark-orange/40 rounded-lg text-spark-eggshell/90 text-sm"
      >
        <strong>Demo placeholder:</strong> The production Live page will embed the
        player and show real-time episode info and quests. This page is for demonstration
        only.
      </div>
    </div>
  </body>
</html>
