/**
 * Legacy notice: Prisma has been removed from the build and runtime paths.
 * This module remains only to avoid import breakage and should not be used.
 * If database access is needed locally, reintroduce Prisma in a separate, non-SSG path.
 */
export const db: never = undefined as never;
