/**
 * Legacy notice: Prisma seed script removed.
 * Static JSON snapshots in `src/data/*.json` are now the source of truth for SSG.
 * This file is intentionally a no-op to keep TypeScript globs happy.
 */
export {};
